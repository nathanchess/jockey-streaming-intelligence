TL_API_KEY=
KNOWLEDGE_STORE_IDS={"french_chef":"","hells_kitchen":"","lizzie_bennet":"","omeleto_reserve":""}

# Vercel Blob — run `npm run upload-playback` to host MP4s for production playback
BLOB_READ_WRITE_TOKEN=

# Runtime secrets for Next.js only — copy to app/.env.local
# GitHub Packages (npm): use repo-root .env.registry — see .env.registry.example
