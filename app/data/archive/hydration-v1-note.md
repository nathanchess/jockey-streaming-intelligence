# Hydration v1 (archived)

`demo-manifest.json` → `hydrated_metadata` and `app/data/jockey-cache/*/hydration.json` remain as the **v1 seed cache** used by Explore/Discover/Program demos.

Library **After Jockey** now prefers **v2** experiment output under `app/data/jockey-v2/`. v1 fields are not deleted.
